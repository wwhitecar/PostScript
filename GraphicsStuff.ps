% William Trevor Whitecar
% Gurka - PPL
% Graphics Display - Rotating Squares

% Basically sets the whole background to back so the squares will 
% 'pop' when they are drawn on the page.

/FillBackground {
       newpath
       0 0 moveto	% Move to start point
       640 0 lineto	
       640 800 lineto	%Draw a square the size of the page.
       0 800 lineto
       0 0 lineto
       closepath
       0 0 0 setrgbcolor  % Set Color to black
       fill		% Fill the square with black color.
} def


% Will save the current graphics and then enter a loop that will loop 
% from 0 to 700 in increments of 2. Will rotate the position with each loop
% and will also rotate the color depending on what increment it is on.

/Squares { 
	gsave
	    0 2 700 {
		    dup			% Duplicate top of stack twice
		    dup
	       	    moveto		% Moveto based on loop interation
	       	    1 exch 0 exch 0 exch % Set rgb colors pushed and setup 
		    			 %stack to roll
		    3 exch roll 	 % Roll the stack to get color change
		    setrgbcolor		% Set the color
		    10  rotate 		%Rotate the squares
	    	    0 70 rlineto	
	    	    70 0 rlineto	%Draw the square
	    	    0 -70 rlineto
	    	    -70 0 rlineto
	    	    closepath
	    	    fill		% Fill square with color
		    stroke
		} for	
	grestore		
} def


% Write my name in the bottem corner of the page.

/Name {
	newpath
	400 0 moveto		% Move to proper place to write name
	
	/Times-Roman findfont	% Set font information
	20 scalefont
	setfont
	1 1 1 setrgbcolor	% Set color to white
	(William Trevor Whitecar) show	% Push name to stack and show it
} def

% Main calls that will fill the background in black, draw the squares, and
% then write my name in the bottem right corner.

FillBackground
Squares
Name
showpage

